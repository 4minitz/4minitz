@startuml
!include style.puml

Moderator -> Router: /minutesadd/:_id (for parent meetingSeries)
    Router -> MeetingSeries: addNewMinutes(callback)
        MeetingSeries -> Minutes: (new Minutes()).save(callback)
    Router <-- Minutes: callback(_id)
Moderator <- Router: redirect(/minutesedit/:_id) (for new minutes)

@enduml